#include "main.h"
/**
 * _abs - function will compute the absolute value of number
 * @n: the number we gonna check
 * Return: the absolute value of number
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
